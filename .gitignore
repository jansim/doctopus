.DS_Store
.build/
.swiftpm/
xcuserdata/

# Everything Scripts/build.sh produces, including the cached actool output
# and the snapshots --uitest writes.
build/

# Generated by Testing/makefixtures.swift.
Testing/DemoLibrary/

# A library's index lives in a visible `*.doctopus` directory inside the folder
# it indexes; never commit one that lands in the tree.
*.doctopus/
